name: Publish to PyPI

on:
  push:
    tags:
      - "v*" # 当推送以 v 开头的 tag 时自动触发
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      target:
        description: "发布目标 (默认正式 PyPI)"
        required: false
        type: choice
        options:
          - pypi
          - testpypi
        default: pypi

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # 用于PyPI的信任发布

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Clean dist directory before build
        run: |
          # 清空 dist 目录，确保只包含本次构建的文件
          rm -rf dist/*
          echo "Cleaned dist directory"

      - name: Build package
        run: |
          python -m build

      - name: List built packages
        run: |
          echo "Built packages:"
          ls -la dist/

      - name: Publish to TestPyPI
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'testpypi'
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}
          TWINE_REPOSITORY_URL: https://test.pypi.org/legacy/
        run: |
          twine upload dist/*

      - name: Publish to PyPI
        if: github.event_name == 'push' || github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && github.event.inputs.target != 'testpypi')
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          packages-dir: dist/
